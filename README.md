# Cabina LPモック

`index.html` をブラウザで開くと確認できます。

## 構成

- `index.html`: セクションの骨組み
- `css/theme.css`: 色・フォントなど共通トークン
- `css/app.css`: レイアウトと見た目
- `assets/`: LPで使う公開用画像

`src/`・`src-tauri/` の外に置いています。秘密鍵、ライセンス台帳、購入者情報はこのフォルダに入れません。
